# School-Management-System-by-Bash-Script
The system was created using Bash Script commands. It contains of two pages, one for the administration and other one for the general users. It was run in Linux.
